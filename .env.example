# Neon Postgres connection string (HTTP-compatible, from your Neon project dashboard)
# Get it via Neon integration: get_connection_string, or the Neon console.
DATABASE_URL="postgresql://user:password@ep-example-000000.region.aws.neon.tech/neondb?sslmode=require"

# Set to "production" on real deployments
NODE_ENV="development"
